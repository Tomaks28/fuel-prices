# SonarQube Cloud (free for public repositories).
# Verify these two against the project's "Information" page: the keys below are
# the ones SonarCloud generates by default when importing a GitHub repository.
sonar.projectKey=Tomaks28_fuel-prices
sonar.organization=tomaks28

sonar.sources=src
sonar.sourceEncoding=UTF-8

# Build output and vendored code are not ours to analyse.
sonar.exclusions=dist/**,node_modules/**,coverage/**

# Populated once a test suite exists:
# sonar.tests=src
# sonar.test.inclusions=**/*.test.ts
# sonar.javascript.lcov.reportPaths=coverage/lcov.info
